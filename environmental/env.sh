#!/usr/bin/env bash


echo "The computer: $(hostname), the user: $USER, the home $HOME"


#Commands (hostname, ls) are actual executable files that must be found and run to produce
#Environment variables ($USER, $HOME) are just stored values that can be accessed without running anything.


